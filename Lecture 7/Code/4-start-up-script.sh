# update the operating system
sudo get update
sudo apt update


# Some commands are interactive and will ask you for input, which could
# interrupt the full execution of the script. You can anticipate basic prompts
# using -y in the line of code
sudo apt install python3-pip -y

# install flask
pip3 install flask
